import 'package:flutter/material.dart';

class AdvisoryPage extends StatelessWidget {
  const AdvisoryPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text('Advisory — Phase 10')),
      );
}
